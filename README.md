# 🚐 VanLife News Aggregator

> Агрегатор новостей о vanlife и автодомах со всего мира

**Домен:** `news.vanlife.bez.coffee`

## 📋 Содержание

- [Возможности](#-возможности)
- [Требования](#-требования)
- [Установка](#-установка)
- [Конфигурация](#-конфигурация)
- [Использование](#-использование)
- [API](#-api)
- [Cron-задачи](#-cron-задачи)
- [Структура проекта](#-структура-проекта)
- [База данных](#-база-данных)
- [Разработка](#-разработка)
- [Changelog](#-changelog)

---

## ✨ Возможности

### Реализовано
- [x] Сбор новостей из Google News RSS (20+ языков)
- [x] Декодирование Google News URL в оригинальные ссылки с кешированием
- [x] Базовая структура фронтенда с выводом новостей
- [x] Переключатель светлой/тёмной темы
- [x] Основные классы ядра (Роутер, Конфиг, БД)
- [x] Система миграций и сидов для БД
- [x] OpenAI-провайдер для chat-completions с ограничением запросов

### В планах
- [ ] ИИ-перевод на русский (OpenAI)
- [ ] Автоматическая категоризация и тегирование
- [ ] Кластеризация (группировка) похожих новостей
- [ ] Фильтры и поиск
- [ ] Панель администратора
- [ ] SEO-оптимизация

---

## 📦 Требования

- PHP 8.2+
- MySQL 8.0+
- Composer 2.x
- Расширения PHP: cURL, DOM, JSON, PDO, mbstring

---

## 🚀 Установка

### 1. Клонирование репозитория
```bash
git clone https://github.com/YOUR_USERNAME/vanlife-news.git
cd vanlife-news
```

### 2. Установка зависимостей
```bash
composer install --no-dev
```

### 3. Настройка окружения
Скопируйте файл с примером переменных окружения и отредактируйте его.
```bash
cp .env.example .env
nano .env
```
Заполните как минимум данные для подключения к базе данных.

### 4. Создание базы данных
Подключитесь к MySQL и создайте базу данных и пользователя.
```sql
CREATE DATABASE vanlife_news CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'vanlife'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON vanlife_news.* TO 'vanlife'@'localhost';
FLUSH PRIVILEGES;
```

### 5. Миграции и начальные данные
Выполните миграцию для создания структуры таблиц и загрузите начальные данные (категории, источники и т.д.).
```bash
php scripts/migrate.php --seed
```

### 6. Настройка веб-сервера

**Пример для Nginx:**
```nginx
server {
    listen 80;
    server_name news.vanlife.bez.coffee;
    root /path/to/vanlife-news/public;
    index index.php;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock; # Укажите ваш сокет
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }

    location ~ /\.ht {
        deny all;
    }
}
```

### 7. Настройка Cron
Добавьте задачу в `crontab` для автоматического сбора новостей.
```cron
# Собирать новости каждые 4 часа
0 */4 * * * php /path/to/vanlife-news/scripts/fetch_news.php >> /var/log/vanlife-fetch.log 2>&1
```

---

## ⚙️ Конфигурация

### Переменные окружения (.env)

| Переменная | Описание | Обязательно |
|----------|-------------|----------|
| `DB_HOST` | Хост БД | ✅ |
| `DB_PORT` | Порт БД | ❌ (по умолч. 3306) |
| `DB_NAME` | Имя БД | ✅ |
| `DB_USER` | Пользователь БД | ✅ |
| `DB_PASS` | Пароль БД | ✅ |
| `OPENAI_API_KEY` | API ключ OpenAI | ✅ (для AI-функций) |
| `OPENAI_MODEL` | Модель OpenAI | ❌ (по умолч. `gpt-4o-mini`) |
| `OPENAI_MAX_TOKENS` | Лимит токенов на ответ | ❌ (по умолч. `1000`) |
| `OPENAI_TEMPERATURE` | Температура выборки | ❌ (по умолч. `0.3`) |
| `OPENAI_REQUESTS_PER_MINUTE` | Ограничение RPS для OpenAI | ❌ (по умолч. `20`) |
| `APP_URL` | URL сайта | ✅ |
| `APP_DEBUG`| Режим отладки | ❌ (по умолч. `false`) |
| `LOG_LEVEL`| Уровень логирования | ❌ (по умолч. `info`) |

---

## 📖 Использование

### CLI-скрипты
Для выполнения фоновых задач используются CLI-скрипты.

- **Сбор новостей:**
  ```bash
  php scripts/fetch_news.php
  ```
- **Миграции БД:**
  ```bash
  php scripts/migrate.php
  php scripts/migrate.php --seed # с начальными данными
  ```

### Google News URL Decoder
Сервис `GoogleNewsUrlDecoder` используется внутри `NewsFetcher` для автоматического преобразования ссылок Google News в оригинальные URL статей. Он не требует ручного вмешательства.

### OpenAI Provider
Класс `App\AI\OpenAIProvider` реализует интерфейс `AIProviderInterface` и предоставляет удобный метод `chat()` для работы с OpenAI Chat Completions. Настройки берутся из `config/config.php` и учитывают ограничение запросов в минуту.

Пример использования:
```php
$provider = new \App\AI\OpenAIProvider($config, $logger);
$response = $provider->chat([
    ['role' => 'system', 'content' => 'Ты — ассистент для vanlife новостей'],
    ['role' => 'user', 'content' => 'Сформулируй заголовок для статьи'],
]);

echo $response->content;
```

---

## 🔌 API

| Метод | URL | Статус | Описание |
|-------|-----|--------|----------|
| GET | `/health` | ✅ | Health-check для мониторинга |
| GET | `/api/news` | ⬜ | Список новостей |
| GET | `/api/news/{id}` | ⬜ | Одна новость |
| GET | `/api/clusters` | ⬜ | Кластеры новостей |

На данный момент реализован только endpoint `/health`. Остальные запланированы на будущие фазы разработки.

---

## ⏰ Cron-задачи

Рекомендуемые настройки для `crontab`:

| Скрипт | Расписание | Описание |
|--------|----------|-------------|
| `fetch_news.php` | `0 */4 * * *` | Сбор новостей из RSS-источников |
| `process_news.php` | `15 * * * *` | ИИ-обработка (будет добавлено) |
| `cluster_news.php` | `30 */6 * * *` | Кластеризация (будет добавлено) |

---

## 📁 Структура проекта

```
vanlife-news/
├── public/          # Document root (точка входа)
├── src/             # Исходный код приложения
│   ├── Core/        # Ядро фреймворка
│   ├── AI/          # Провайдеры ИИ и вспомогательные классы
│   ├── Controller/  # Контроллеры
│   ├── Service/     # Сервисы (бизнес-логика)
│   ├── Model/       # Модели данных
│   ├── Repository/  # Репозитории (доступ к данным)
│   └── ...
├── templates/       # HTML-шаблоны
├── config/          # Конфигурационные файлы
├── scripts/         # CLI-скрипты (cron)
├── sql/             # SQL-схемы, миграции, сиды
├── logs/            # Лог-файлы
└── ...
```

---

## 🗄️ База данных

Схема базы данных описана в файле `sql/schema.sql`. Миграции и начальные данные находятся в `sql/migrations/` и `sql/seeds/`.

Для применения миграций используйте скрипт:
```bash
php scripts/migrate.php
```

---

## 🛠️ Разработка

### Прогресс
Подробный трекер задач находится в файле [PROGRESS.md](PROGRESS.md).

### Текущая фаза
**PHASE 2: AI Processing** — интеграция ИИ-обработки новостей.

### Участие в разработке
1. Изучите `PROGRESS.md`, чтобы определить текущую задачу.
2. Следуйте стандартам кодирования проекта.
3. Обновляйте документацию при внесении изменений.
4. Создавайте осмысленные коммиты.

---

## 📝 Changelog

История изменений ведётся в файле [CHANGELOG.md](CHANGELOG.md).
