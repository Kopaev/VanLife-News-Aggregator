# 🚐 VanLife News Aggregator

> Агрегатор новостей о vanlife и автодомах со всего мира

**Домен:** `news.vanlife.bez.coffee`

## 📋 Содержание

- [Возможности](#-возможности)
- [Требования](#-требования)
- [Установка](#-установка)
- [Конфигурация](#-конфигурация)
- [Использование](#-использование)
- [API](#-api)
- [Cron-задачи](#-cron-задачи)
- [Структура проекта](#-структура-проекта)
- [База данных](#-база-данных)
- [Разработка](#-разработка)
- [Changelog](#-changelog)

---

## ✨ Возможности

### Реализовано
- [x] Сбор новостей из Google News RSS (20+ языков)
- [x] Декодирование Google News URL в оригинальные ссылки с кешированием
- [x] Базовая структура фронтенда с выводом новостей
- [x] Переключатель светлой/тёмной темы
- [x] Основные классы ядра (Роутер, Конфиг, БД)
- [x] Система миграций и сидов для БД
- [x] OpenAI-провайдер для chat-completions с ограничением запросов
- [x] NewsProcessor для AI-оценки релевантности, категорий/страны и тегов
- [x] ModerationService для дополнительной фильтрации опасного контента
- [x] TranslationService для перевода статей на русский и сохранения в `articles`/`translations`
- [x] ClusteringService для оценки схожести статей перед объединением в кластеры
- [x] ClusterManager/ClusterRepository для создания кластеров и авто-присоединения похожих статей
- [x] Авто-выбор главной статьи кластера по релевантности, свежести, переводу, изображению и просмотрам
- [x] Публичный UI кластеров: карточки на главной, страница списка и страница кластера
- [x] Русские шаблоны: вывод перевода/саммари, категории, страны, теги и статус статьи
- [x] Оптимизация токенов: очистка и обрезка промптов, лимиты `max_tokens` в AI-запросах
- [x] Система фильтрации новостей и кластеров (категории, страны, языки, период)
- [x] API endpoints для фильтров и получения данных
- [x] Динамическая загрузка новостей без перезагрузки страницы
- [x] Пагинация с сохранением фильтров в URL

### В планах
- [ ] Панель администратора
- [ ] SEO-оптимизация

---

## 📦 Требования

- PHP 8.2+
- MySQL 8.0+
- Composer 2.x
- Расширения PHP: cURL, DOM, JSON, PDO, mbstring

---

## 🚀 Установка

### 1. Клонирование репозитория
```bash
git clone https://github.com/YOUR_USERNAME/vanlife-news.git
cd vanlife-news
```

### 2. Установка зависимостей
```bash
composer install --no-dev
```

### 3. Настройка окружения
Скопируйте файл с примером переменных окружения и отредактируйте его.
```bash
cp .env.example .env
nano .env
```
Заполните как минимум данные для подключения к базе данных.

### 4. Создание базы данных
Подключитесь к MySQL и создайте базу данных и пользователя.
```sql
CREATE DATABASE vanlife_news CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'vanlife'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON vanlife_news.* TO 'vanlife'@'localhost';
FLUSH PRIVILEGES;
```

### 5. Миграции и начальные данные
Выполните миграцию для создания структуры таблиц и загрузите начальные данные (категории, источники и т.д.).
```bash
php scripts/migrate.php --seed
```

### 6. Настройка веб-сервера

**Пример для Nginx:**
```nginx
server {
    listen 80;
    server_name news.vanlife.bez.coffee;
    root /path/to/vanlife-news/public;
    index index.php;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock; # Укажите ваш сокет
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }

    location ~ /\.ht {
        deny all;
    }
}
```

### 7. Настройка Cron
Добавьте задачу в `crontab` для автоматического сбора новостей.
```cron
# Собирать новости каждые 4 часа
0 */4 * * * php /path/to/vanlife-news/scripts/fetch_news.php >> /var/log/vanlife-fetch.log 2>&1
```

---

## ⚙️ Конфигурация

### Переменные окружения (.env)

| Переменная | Описание | Обязательно |
|----------|-------------|----------|
| `DB_HOST` | Хост БД | ✅ |
| `DB_PORT` | Порт БД | ❌ (по умолч. 3306) |
| `DB_NAME` | Имя БД | ✅ |
| `DB_USER` | Пользователь БД | ✅ |
| `DB_PASS` | Пароль БД | ✅ |
| `OPENAI_API_KEY` | API ключ OpenAI | ✅ (для AI-функций) |
| `OPENAI_MODEL` | Модель OpenAI | ❌ (по умолч. `gpt-4o-mini`) |
| `OPENAI_MAX_TOKENS` | Лимит токенов на ответ | ❌ (по умолч. `1000`) |
| `OPENAI_TEMPERATURE` | Температура выборки | ❌ (по умолч. `0.3`) |
| `PROMPT_RELEVANCE_TITLE_LIMIT` | Максимум символов в заголовке для промпта релевантности | ❌ (по умолч. `240`) |
| `PROMPT_RELEVANCE_SUMMARY_LIMIT` | Максимум символов в описании для промпта релевантности | ❌ (по умолч. `1200`) |
| `PROMPT_RELEVANCE_MAX_TOKENS` | Лимит токенов ответа для AI-оценки релевантности | ❌ (по умолч. `300`) |
| `PROMPT_TRANSLATE_TITLE_LIMIT` | Максимум символов в заголовке для промпта перевода | ❌ (по умолч. `400`) |
| `PROMPT_TRANSLATE_SUMMARY_LIMIT` | Максимум символов в описании для промпта перевода | ❌ (по умолч. `1200`) |
| `PROMPT_TRANSLATE_MAX_TOKENS` | Лимит токенов ответа для перевода | ❌ (по умолч. `300`) |
| `OPENAI_REQUESTS_PER_MINUTE` | Ограничение RPS для OpenAI | ❌ (по умолч. `20`) |
| `APP_URL` | URL сайта | ✅ |
| `APP_DEBUG`| Режим отладки | ❌ (по умолч. `false`) |
| `FETCH_ENABLED` | Включить крон сбора новостей | ❌ (по умолч. `true`) |
| `PROCESS_ENABLED` | Включить крон AI-обработки | ❌ (по умолч. `true`) |
| `PROCESS_RELEVANCE_BATCH` | Количество статей за один проход AI-разметки | ❌ (по умолч. `10`) |
| `PROCESS_TRANSLATION_BATCH` | Количество статей в очереди перевода | ❌ (по умолч. `10`) |
| `PROCESS_MODERATION_BATCH` | Количество статей для быстрой модерации | ❌ (по умолч. `20`) |
| `CLUSTER_MIN_SIMILARITY` | Минимальный порог схожести статей для объединения | ❌ (по умолч. `0.55`) |
| `CLUSTER_CANDIDATE_WINDOW_HOURS` | Окно публикаций для поиска похожих статей (в часах) | ❌ (по умолч. `120`) |
| `CLUSTER_TIME_DECAY_HOURS` | Полураспад веса при большом разрыве по времени публикации | ❌ (по умолч. `72`) |
| `CLUSTER_SUMMARY_LIMIT` | Ограничение длины саммари при расчёте схожести | ❌ (по умолч. `800`) |
| `CLUSTER_BATCH_SIZE` | Количество бесхозных статей, обрабатываемых за запуск кластеризации | ❌ (по умолч. `20`) |
| `CLUSTER_CANDIDATES_LIMIT` | Максимум кандидатов, которые сравниваются на схожесть для каждой статьи | ❌ (по умолч. `80`) |
| `CLUSTER_ATTACH_LIMIT` | Сколько бесхозных статей можно сразу присоединить к кластеру | ❌ (по умолч. `5`) |
| `CLUSTER_MAIN_HALFLIFE_HOURS` | Полураспад свежести при выборе главной статьи кластера | ❌ (по умолч. `72`) |
| `CLUSTER_MAIN_TRANSLATION_BONUS` | Бонус к статье с русским переводом | ❌ (по умолч. `12`) |
| `CLUSTER_MAIN_IMAGE_BONUS` | Бонус за наличие изображения | ❌ (по умолч. `6`) |
| `CLUSTER_MAIN_PUBLISHED_BONUS` | Бонус за статус `published` (выше, чем `moderation`) | ❌ (по умолч. `8`) |
| `CLUSTER_MAIN_VIEWS_WEIGHT` | Вес одного просмотра при выборе главной статьи | ❌ (по умолч. `0.05`) |
| `CLUSTER_MAIN_VIEWS_CAP` | Сколько просмотров учитывать максимум (анти-спайк) | ❌ (по умолч. `200`) |
| `CLUSTER_MAIN_RELEVANCE_FLOOR` | Базовое значение, если нет AI-оценки релевантности | ❌ (по умолч. `50`) |
| `LOG_LEVEL`| Уровень логирования | ❌ (по умолч. `info`) |

---

## 📖 Использование

### CLI-скрипты
Для выполнения фоновых задач используются CLI-скрипты.

- **Сбор новостей:**
  ```bash
  php scripts/fetch_news.php
  ```
- **AI-обработка + перевод + модерация:**
  ```bash
  php scripts/process_news.php
  ```
- **Миграции БД:**
  ```bash
  php scripts/migrate.php
  php scripts/migrate.php --seed # с начальными данными
  ```

### Публичные маршруты
- `/` — лента свежих статей + превью кластеров (с фильтрами).
- `/news/{slug}` — подробная страница статьи.
- `/clusters` — список кластеров с основными метаданными и ссылками.
- `/clusters/{slug}` — страница конкретного кластера с лентой его новостей.

### Фильтрация новостей
На главной странице доступна система фильтров:
- **По периоду**: Сегодня, Неделя, Месяц, Все
- **По категории**: Законы, Запреты, Открытия, Закрытия, Происшествия и др.
- **По стране**: Германия, Франция, США и др. (только страны с активными новостями)
- **По языку оригинала**: Немецкий, Английский, Французский и др.

Фильтры работают без перезагрузки страницы, состояние сохраняется в URL. Поддерживается пагинация с сохранением активных фильтров.

### Google News URL Decoder
Сервис `GoogleNewsUrlDecoder` используется внутри `NewsFetcher` для автоматического преобразования ссылок Google News в оригинальные URL статей. Он не требует ручного вмешательства.

### OpenAI Provider
Класс `App\AI\OpenAIProvider` реализует интерфейс `AIProviderInterface` и предоставляет удобный метод `chat()` для работы с OpenAI Chat Completions. Настройки берутся из `config/config.php` и учитывают ограничение запросов в минуту.

Пример использования:
```php
$provider = new \App\AI\OpenAIProvider($config, $logger);
$response = $provider->chat([
    ['role' => 'system', 'content' => 'Ты — ассистент для vanlife новостей'],
    ['role' => 'user', 'content' => 'Сформулируй заголовок для статьи'],
]);

echo $response->content;
```

### NewsProcessor (ИИ-оценка релевантности и разметки)
Сервис `App\Service\NewsProcessor` использует OpenAI для определения релевантности новостей vanlife-аудитории и разметки материалов метаданными. На основе ответа модели выбирается статус статьи и сохраняются:

- `published` — высокий балл (≥ авто-порога)
- `moderation` — спорные или потенциально опасные материалы
- `rejected` — нерелевантные материалы или автоправила отклонения

Дополнительно сохраняются:

- `category_slug` — одна из категорий из `config/categories.php` (fallback `other`)
- `country_code` — ISO-код страны (если определён)
- `tags` — список тегов, предоставленных моделью

Пороговые значения и ключевые слова для автоматического отклонения/модерации задаются в `config/moderation.php`.

Пример использования в скрипте обработки:
```php
$processor = new \App\Service\NewsProcessor($openAiProvider, $logger, $articleRepository, $config);
$processed = $processor->processRelevance(10); // обработать до 10 свежих статей

echo "Processed: {$processed}";
```

### ModerationService (фильтрация контента)
`App\Service\ModerationService` применяет правила из `config/moderation.php`, чтобы пометить статьи с потенциально опасными ключевыми словами или автоматически отклонить их. Сервис обрабатывает материалы, прошедшие AI-разметку, и проставляет `moderated_at`, обновляя статус и причину модерации.

Логика по умолчанию:
- слова из `auto_reject` → статус `rejected` и причина `auto_reject_keyword`;
- слова из `require_moderation` → статус `moderation` и причина `keyword_flag`;
- публикации с низким баллом, но статусом `published` → возвращаются в `moderation` с причиной `below_threshold`.

Пример использования:
```php
$moderationService = new \App\Service\ModerationService($articleRepository, $logger);
$moderated = $moderationService->moderatePending(20); // прогнать 20 свежих статей

echo "Moderated: {$moderated}";
```

### TranslationService (ИИ-перевод на русский)
Сервис `App\Service\TranslationService` переводит обработанные статьи на русский язык. Он подбирает материалы с ненулевой AI-разметкой (`ai_processed_at`), статусом `published`/`moderation` и отсутствующим переводом на русском. Для каждого элемента сервис:

- отправляет в OpenAI заголовок и описание с указанием исходного языка;
- сохраняет перевод в полях `articles.title_ru` и `articles.summary_ru`;
- кэширует запись в таблице `translations` (target_language = `ru`);
- пишет результат в лог.

Пример использования:
```php
$translator = new \App\Service\TranslationService($openAiProvider, $logger, $articleRepository, $translationRepository, $config);
$done = $translator->translatePending(5); // перевести 5 статей без русской версии

echo "Translated: {$done}";
```

### ClusteringService (оценка схожести)
`App\Service\ClusteringService` вычисляет коэффициент схожести между статьями, комбинируя:

- пересечение токенов заголовков и саммари (после очистки и стоп-слов);
- совпадение тегов (JSON или массив);
- бонусы за совпадение категории/страны;
- временной декей для публикаций, вышедших слишком далеко друг от друга.

Порог `min_similarity`, окно публикаций и веса компонентов настраиваются в `config/clustering.php` или через переменные окружения
(`CLUSTER_MIN_SIMILARITY`, `CLUSTER_CANDIDATE_WINDOW_HOURS`, `CLUSTER_TIME_DECAY_HOURS`, `CLUSTER_SUMMARY_LIMIT`).

Пример расчёта похожих статей для одной публикации:
```php
$clustering = new \App\Service\ClusteringService($config, $logger);
$similar = $clustering->findSimilar($article, $candidatePool); // вернёт отсортированный список с id и score
```

### ClusterManager (создание и обновление кластеров)
`App\Service\ClusterManager` объединяет статьи без кластера:

- забирает очередь статей без `cluster_id` батчами (`CLUSTER_BATCH_SIZE`);
- ищет кандидатов по окну публикаций и считает схожесть через `ClusteringService`;
- если найден кластер с наибольшей средней схожестью — привязывает статью и может автоматически прикрепить несколько бесхозных похожих материалов (`CLUSTER_ATTACH_LIMIT`);
- если подходящего кластера нет — создаёт новый (`ClusterRepository` генерирует slug, считает страны/дату и сохраняет счётчик статей);
- после операций пересчитывает статистику кластера (счётчик, даты, страны, популярную категорию).

Настройки в `config/clustering.php` и `.env`:

- `CLUSTER_BATCH_SIZE` — сколько статей без кластера обрабатывать за проход;
- `CLUSTER_CANDIDATES_LIMIT` — сколько кандидатов брать для расчёта схожести;
- `CLUSTER_ATTACH_LIMIT` — сколько бесхозных статей можно сразу прикрепить к найденному кластеру;
- базовые параметры схожести (`CLUSTER_MIN_SIMILARITY`, `CLUSTER_CANDIDATE_WINDOW_HOURS`, `CLUSTER_TIME_DECAY_HOURS`, `CLUSTER_SUMMARY_LIMIT`).

Пример ручного запуска батча кластеризации:
```php
$mainSelector = new \App\Service\ClusterMainSelector($config, $logger);
$manager = new \App\Service\ClusterManager($clusteringService, $articleRepository, $clusterRepository, $mainSelector, $logger, $config);
$done = $manager->clusterUnassigned();

echo "Clustered: {$done}";
```

### Выбор главной статьи кластера
`App\Service\ClusterMainSelector` автоматически назначает `clusters.main_article_id` после любого изменения состава кластера. На итоговый балл влияет:

- AI-оценка релевантности (`ai_relevance_score`), минимальный порог задаётся `CLUSTER_MAIN_RELEVANCE_FLOOR`;
- свежесть публикации с экспоненциальным затуханием (половинный период — `CLUSTER_MAIN_HALFLIFE_HOURS`);
- наличие перевода на русский (`CLUSTER_MAIN_TRANSLATION_BONUS`), изображения (`CLUSTER_MAIN_IMAGE_BONUS`) и статус `published` (`CLUSTER_MAIN_PUBLISHED_BONUS`);
- просмотры: взвешиваются по `CLUSTER_MAIN_VIEWS_WEIGHT` и ограничиваются `CLUSTER_MAIN_VIEWS_CAP`.

Если после расчёта нет подходящих статей (пустой кластер), поле `main_article_id` не меняется.

### Оптимизация расхода токенов

- Заголовок и описание перед отправкой в OpenAI проходят через `TextSanitizer`, который убирает HTML, лишние пробелы и обрезает строки по заданным лимитам.
- Лимиты промптов и `max_tokens` управляются переменными `.env` (`PROMPT_RELEVANCE_*`, `PROMPT_TRANSLATE_*`).
- `NewsProcessor` и `TranslationService` передают явный `max_tokens`, чтобы ограничить размер ответа модели.

### Комплексный AI-пайплайн (cron)
Запускается скриптом `scripts/process_news.php` и последовательно выполняет три этапа:

1. **AI-разметка релевантности** (`NewsProcessor`) — выбирает свежие статьи со статусом `new`.
2. **Перевод на русский** (`TranslationService`) — обрабатывает опубликованные/модерируемые материалы без перевода.
3. **Пост-модерация** (`ModerationService`) — применяет ключевые слова и пороговые значения.

Размер батчей на каждом этапе настраивается переменными `.env`: `PROCESS_RELEVANCE_BATCH`, `PROCESS_TRANSLATION_BATCH`, `PROCESS_MODERATION_BATCH`.

### 🔐 Публичный интерфейс (шаблоны)
Главные страницы теперь локализованы и отображают AI-метаданные:
- `templates/pages/home.php` выводит русские заголовки/саммари (fallback на оригинал), бейдж категории с иконкой и цветом, страну (флаг + название), статус публикации, балл релевантности AI, теги и ссылку на оригинальный источник.
- `templates/pages/article.php` показывает те же метаданные, отдельные блоки перевода и оригинала, причину модерации (если есть) и название RSS-источника.
- `templates/pages/clusters.php` — сетка кластеров с категориями, странами, количеством статей и ссылками на главную статью.
- `templates/pages/cluster.php` — страница кластера с метаданными (даты, страны, категория) и лентой статей внутри кластера.
- Вся навигация и тексты интерфейса локализованы на русский язык.

---

## 🔌 API

| Метод | URL | Статус | Описание |
|-------|-----|--------|----------|
| GET | `/health` | ✅ | Health-check для мониторинга |
| GET | `/api/filters` | ✅ | Данные для фильтров (категории, страны, языки) |
| GET | `/api/news` | ✅ | Список новостей с фильтрацией и пагинацией |
| GET | `/api/clusters` | ✅ | Кластеры новостей с фильтрацией и пагинацией |

### API Endpoints

#### GET /api/filters
Возвращает доступные опции фильтрации.

**Ответ:**
```json
{
  "categories": [
    {"slug": "law", "name": "Законы и правила", "icon": "⚖️", "color": "#007bff", "count": 15}
  ],
  "countries": [
    {"code": "DE", "name": "Германия", "flag": "🇩🇪", "count": 42}
  ],
  "languages": [
    {"code": "de", "name": "Немецкий", "name_native": "Deutsch", "count": 35}
  ]
}
```

#### GET /api/news
Возвращает отфильтрованные новости с пагинацией.

**Параметры запроса:**
- `category` - slug категории (опционально)
- `country` - код страны (опционально)
- `language` - код языка оригинала (опционально)
- `period` - период (today, week, month, year, опционально)
- `page` - номер страницы (по умолчанию 1)
- `limit` - количество на странице (по умолчанию 20, максимум 100)

**Пример запроса:**
```
GET /api/news?category=law&country=DE&period=week&page=1&limit=20
```

**Ответ:**
```json
{
  "articles": [...],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 145,
    "pages": 8
  },
  "filters": {
    "category": "law",
    "country": "DE",
    "language": null,
    "period": "week"
  }
}
```

#### GET /api/clusters
Возвращает отфильтрованные кластеры с пагинацией.

**Параметры запроса:**
- `category` - slug категории (опционально)
- `country` - код страны (опционально)
- `page` - номер страницы (по умолчанию 1)
- `limit` - количество на странице (по умолчанию 20, максимум 100)

**Пример запроса:**
```
GET /api/clusters?category=ban&country=FR&page=1
```

---

## ⏰ Cron-задачи

Рекомендуемые настройки для `crontab`:

| Скрипт | Расписание | Описание |
|--------|----------|-------------|
| `fetch_news.php` | `0 */4 * * *` | Сбор новостей из RSS-источников |
| `process_news.php` | `15 * * * *` | AI-пайплайн: релевантность, перевод на русский, модерация (лимиты на батчи в `.env`) |
| `cluster_news.php` | `30 */6 * * *` | Кластеризация бесхозных статей в кластеры (опциональный аргумент — лимит статей в батче) |

Запуск вручную:

```bash
php scripts/cluster_news.php [limit]
```

Скрипт отключается через `CLUSTER_ENABLED=false` в `.env`. Если указать необязательный `limit`, он переопределит `CLUSTER_BATCH_SIZE` на один запуск.

---

## 📁 Структура проекта

```
vanlife-news/
├── public/          # Document root (точка входа)
├── src/             # Исходный код приложения
│   ├── Core/        # Ядро фреймворка
│   ├── AI/          # Провайдеры ИИ и вспомогательные классы
│   ├── Controller/  # Контроллеры
│   ├── Service/     # Сервисы (бизнес-логика)
│   ├── Model/       # Модели данных
│   ├── Repository/  # Репозитории (доступ к данным)
│   └── ...
├── templates/       # HTML-шаблоны
├── config/          # Конфигурационные файлы
├── scripts/         # CLI-скрипты (cron)
├── sql/             # SQL-схемы, миграции, сиды
├── logs/            # Лог-файлы
└── ...
```

---

## 🗄️ База данных

Схема базы данных описана в файле `sql/schema.sql`. Миграции и начальные данные находятся в `sql/migrations/` и `sql/seeds/`.

Для применения миграций используйте скрипт:
```bash
php scripts/migrate.php
```

---

## 🛠️ Разработка

### Прогресс
Подробный трекер задач находится в файле [PROGRESS.md](PROGRESS.md).

### Текущая фаза
**PHASE 4: Фильтры и поиск** — следующая итерация после завершения кластеризации.

### Участие в разработке
1. Изучите `PROGRESS.md`, чтобы определить текущую задачу.
2. Следуйте стандартам кодирования проекта.
3. Обновляйте документацию при внесении изменений.
4. Создавайте осмысленные коммиты.

---

## 📝 Changelog

История изменений ведётся в файле [CHANGELOG.md](CHANGELOG.md).

### Последние изменения

#### [2025-12-06] - PHASE 4: Фильтры и поиск
**Добавлено:**
- ✨ API endpoints: `/api/filters`, `/api/news`, `/api/clusters`
- ✨ Система фильтрации по категориям, странам, языкам и периоду
- ✨ Динамическая загрузка новостей без перезагрузки страницы
- ✨ Пагинация с сохранением фильтров в URL
- ✨ UI компонент фильтров с адаптивным дизайном
- ✨ JavaScript-модуль `filters.js` для управления фильтрами

**Изменено:**
- 🔄 `HomeController` теперь загружает данные фильтров
- 🔄 `ArticleRepository` и `ClusterRepository` с методами фильтрации
- 🔄 Главная страница интегрирована с компонентом фильтров
